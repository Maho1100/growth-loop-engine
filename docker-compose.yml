services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: gle
      POSTGRES_PASSWORD: gle
      POSTGRES_DB: growth_loop
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
